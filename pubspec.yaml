name: overnites
description: Overnites App
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: 3.3.10

dependencies:
  cookie_jar: ^3.0.1
  dio: ^5.0.0
  dio_cookie_manager: ^2.1.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_secure_storage: ^8.0.0
  flutter_svg: ^2.0.1
  hive_flutter: ^1.1.0
  intl: ^0.17.0
  modal_bottom_sheet: ^3.0.0-pre
  pretty_dio_logger: ^1.3.1
  url_launcher: ^6.1.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/
